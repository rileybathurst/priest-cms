// .config/plugins.js

module.exports = ({ env }) => ({
  upload: {
    enabled: false, // does't work, plugin stays enabled
  },
  email: {
    endabled: false, // does't work, plugin stays enabled
  },
});